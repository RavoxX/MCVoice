# MCVoice for Minecraft 1.8.8

Generated by `tools/port-version/port.py 1.8.8` from `main`.
Adapter family `legacy`, Java 8. Each loader is a standalone Gradle build:

* `forge/`: build generation `unimined`, Gradle 8.14.3 on JDK 21 (`cd minecraft/forge && ./gradlew build`; jar in `minecraft/forge/build/libs/`)
* legacyfabric: not built - not implemented: the Legacy Fabric loader exists, but Legacy Fabric API is not published for this version and the MCVoice Legacy Fabric adapter needs it (keys, ticks, lifecycle)
