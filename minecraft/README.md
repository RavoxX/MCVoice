# MCVoice for Minecraft 1.10

Generated by `tools/port-version/port.py 1.10` from `main`.
Adapter family `legacy`, Java 8. Each loader is a standalone Gradle build:

* `forge/`: build generation `legacyforge`, Gradle 8.8 on JDK 17 (`cd minecraft/forge && ./gradlew build`; jar in `minecraft/forge/build/libs/`)
* fabric: not built - No compatible official Fabric or Legacy Fabric loader exists for this version
