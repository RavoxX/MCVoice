# MCVoice for Minecraft 1.11.2

Generated by `tools/port-version/port.py 1.11.2` from `main`.
Adapter family `legacy`, Java 8. Each loader is a standalone Gradle build:

* `legacyfabric/`: build generation `legacyfabric`, Gradle 9.4.0 on JDK 21 (`cd minecraft/legacyfabric && ./gradlew build`; jar in `minecraft/legacyfabric/build/libs/`)
* `forge/`: build generation `legacyforge`, Gradle 8.8 on JDK 17 (`cd minecraft/forge && ./gradlew build`; jar in `minecraft/forge/build/libs/`)
